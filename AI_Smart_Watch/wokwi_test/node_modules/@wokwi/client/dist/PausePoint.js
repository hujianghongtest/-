export class PausePoint {
    constructor(id, params) {
        this.id = id;
        this.params = params;
        this.promise = new Promise((resolve) => {
            this._resolve = resolve;
        });
    }
    resolve(info) {
        this._resolve(info);
    }
}
