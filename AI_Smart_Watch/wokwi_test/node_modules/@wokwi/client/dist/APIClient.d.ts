import type { APIError, APIEvent, APIHello, APIResponse, APISimStartParams, PinReadResponse, VCDReadResponse } from './APITypes.js';
import { PausePoint, type PausePointParams } from './PausePoint.js';
import { ITransport } from './transport/ITransport.js';
export declare class APIClient {
    private readonly transport;
    private lastId;
    private lastPausePointId;
    private closed;
    private _running;
    private _lastNanos;
    private readonly apiEvents;
    private readonly pausePoints;
    private readonly pendingCommands;
    readonly connected: Promise<void>;
    onConnected?: (helloMessage: APIHello) => void;
    onError?: (error: APIError) => void;
    constructor(transport: ITransport);
    fileUpload(name: string, content: string | Uint8Array): Promise<unknown>;
    fileDownload(name: string): Promise<string | Uint8Array>;
    simStart(params: APISimStartParams): Promise<unknown>;
    simPause(): Promise<unknown>;
    simResume(pauseAfter?: number): Promise<unknown>;
    simRestart({ pause }?: {
        pause?: boolean;
    }): Promise<unknown>;
    simStatus(): Promise<{
        running: boolean;
        nanos: number;
    }>;
    serialMonitorListen(): Promise<unknown>;
    serialMonitorWrite(bytes: number[] | Uint8Array): Promise<unknown>;
    get pausedPromise(): Promise<void> | Promise<APIEvent<any>>;
    framebufferRead(partId: string): Promise<{
        png: string;
    }>;
    controlSet(partId: string, control: string, value: number): Promise<unknown>;
    touchEvent(partId: string, x: number, y: number, event: 'press' | 'release' | 'move', options?: {
        releaseAfter?: number;
    }): Promise<unknown>;
    pinRead(partId: string, pin: string): Promise<PinReadResponse>;
    readVCD(): Promise<VCDReadResponse>;
    addPausePoint(params: PausePointParams, resume?: boolean): Promise<PausePoint<any>>;
    removePausePoint(pausePoint: PausePoint): Promise<boolean>;
    atNanos(nanos: number): Promise<void>;
    delay(nanos: number): Promise<void>;
    waitForSerialBytes(bytes: number[] | Uint8Array): Promise<void>;
    waitForChipOutput(text: string): Promise<void>;
    sendCommand<T = unknown>(command: string, params?: any): Promise<T>;
    get running(): boolean;
    get lastNanos(): number;
    processMessage(message: APIError | APIHello | APIEvent | APIResponse): void;
    processEvent(message: APIEvent): void;
    listen(event: string, listener: (event: APIEvent) => void, options?: AddEventListenerOptions): () => void;
    processResponse(message: APIResponse): void;
    close(): void;
    private handleTransportClose;
    private handleTransportError;
}
